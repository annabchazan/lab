cores = []
meias=0
for x in range (0,4):
    cores.append(int(input('teste')))

y=0
while(y<4):
    if((y+1<4)):
        if cores[y] == cores[y + 1]:
            meias += 1
        y+=1
    else:break
print(meias)



cores = []
meias=0
for x in range (0,4):
     cores.append(int(input('teste')))

y=0
while(y<4):
    for i in range((1+y),4):
        if cores[y] == cores[i]:
            meias += 1
    y+=1

print(meias)
